package basic

fun main() {
    // Для обьявления переменной используются var или val
    // val - аналог final
    // Предпочтительнее везде использовать val вместо var
    // Сначала имя переменной, затем через двоеточие тип
    val name = "John";
    var age: Int = 30

    print(name)
}